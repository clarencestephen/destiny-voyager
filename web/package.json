{
  "name": "destiny-voyager-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "bake": "node scripts/bake-slim-manifest.mjs && node scripts/bake-armor.mjs && node scripts/bake-activities.mjs",
    "prebuild": "npm run bake || echo '[prebuild] manifest cache not found - keeping existing baked files'",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "tsc -b --noEmit",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name=destiny-voyager",
    "deploy:worker": "cd worker && wrangler deploy",
    "deploy": "npm run deploy:worker && npm run deploy:pages"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.1.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.1",
    "lucide-react": "^0.460.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.0.0",
    "tailwind-merge": "^2.5.4",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "wrangler": "^3.86.0"
  }
}
